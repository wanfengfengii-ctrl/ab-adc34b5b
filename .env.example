# Host port to publish the API on (container always listens on 8080).
HOST_PORT=8080
# Secret key signing result certificates. MUST be overridden outside dev.
APP_CERTIFICATE_SECRET=change-me-to-a-long-random-secret
