# Host port published by docker compose (container always listens on 8080).
INDEXING_PORT=8080

# HMAC secret for result certificates. MUST be overridden outside development.
CERT_SECRET=dev-only-cert-secret-change-me
